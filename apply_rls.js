const { createClient } = require('@supabase/supabase-js'); require('dotenv').config(); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); sb.rpc('exec_sql', { sql: ALTER TABLE public.camlenio_banks ENABLE ROW LEVEL SECURITY; DROP POLICY IF EXISTS "Enable read access for all users" ON public.camlenio_banks; CREATE POLICY "Enable read access for all users" ON public.camlenio_banks FOR SELECT USING (true); }).then(console.log);
